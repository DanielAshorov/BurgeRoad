import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme: any) => ({
  toolTip: {
    backgroundColor: "#fff",
    maxWidth: "none",
    color: "white",
    padding: "0px",
  },
}));
