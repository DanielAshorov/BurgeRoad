import { makeStyles } from "@mui/styles";

export const useStyles = makeStyles((theme: any) => ({
  border: {
    position: "absolute",
    width: "29vw",
    height: "91px",
    border: "0.5px solid #cfbfbf",
    boxShadow: " 12px 0 40px 0 rgba(181, 181, 181, 0.3)",
  },
}));
